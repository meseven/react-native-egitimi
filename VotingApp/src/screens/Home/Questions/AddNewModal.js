import React from "react";
import { View, Text, StyleSheet } from "react-native";

const AddNewModal = () => {
	return (
		<View style={styles.container}>
			<Text>AddNewModal</Text>
		</View>
	);
};

const styles = StyleSheet.create({
	container: {
		padding: 10,
	},
});

export default AddNewModal;
